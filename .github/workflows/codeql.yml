name: CodeQL Security Scan

on:
  pull_request:
    branches:
      - dev
      - uat
      - main
  push:
    branches:
      - dev
      - main
      - uat
  schedule:
    - cron: "0 2 * * 1"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name != 'schedule' }}

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  CODEQL_DISABLE_TELEMETRY: true

permissions:
  contents: read
  security-events: write
  pull-requests: write
  issues: write
  actions: read

jobs:
  # ─────────────────────────────────────────────
  # 1. Detect changes
  # ─────────────────────────────────────────────
  detect-changes:
    name: Detect File Changes
    runs-on: ubuntu-latest
    outputs:
      run-codeql: ${{ steps.filter.outputs.run-codeql }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for Python or JS changes
        id: filter
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            CHANGED_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD)
          else
            CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD)
          fi

          echo "Changed files:"
          echo "$CHANGED_FILES"

          if echo "$CHANGED_FILES" | grep -qE '\.(py|js|jsx|ts|tsx)$'; then
            echo "run-codeql=true" >> $GITHUB_OUTPUT
            echo "✅ Python/JS files detected — CodeQL will run"
          else
            echo "run-codeql=false" >> $GITHUB_OUTPUT
            echo "⏭️ No Python/JS files changed — skipping CodeQL"
          fi

  # ─────────────────────────────────────────────
  # 2. CodeQL analysis
  # ─────────────────────────────────────────────
  codeql-analysis:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.run-codeql == 'true'

    strategy:
      fail-fast: false
      matrix:
        language:
          - javascript
          - python

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: security-extended
          config: |
            paths-ignore:
              - '**/node_modules/**'
              - '**/vendor/**'
              - '**/dist/**'
              - '**/build/**'
              - '**/__pycache__/**'
              - '**/*.min.js'
              - '**/*.bundle.js'
              - '**/test/**'
              - '**/tests/**'
              - '**/*.test.js'
              - '**/*.spec.js'
              - '**/*.test.ts'
              - '**/*.spec.ts'
              - '**/test_*.py'
              - '**/*_test.py'
              - '**/migrations/**'
              - '**/fixtures/**'
              - '**/mock/**'
              - '**/mocks/**'
              - '**/generated/**'
              - '**/*.generated.js'
              - '**/coverage/**'
              - '**/.venv/**'
              - '**/venv/**'

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
          upload: always

  # ─────────────────────────────────────────────
  # 3. Security gate — always runs so GitHub
  #    branch protection is never left hanging.
  #    • Skipped CodeQL  → early-exit success
  #    • No findings     → success
  #    • Critical found  → failure (blocks merge)
  # ─────────────────────────────────────────────
  security-gate:
    name: Security Gate / Block Critical
    runs-on: ubuntu-latest
    needs: [detect-changes, codeql-analysis]
    timeout-minutes: 10
    # always() ensures this job runs even when codeql-analysis was skipped.
    # Without always(), a skipped dependency causes this job to be skipped too,
    # which GitHub branch protection treats as "not passed" → PR blocked forever.
    if: always() && (github.event_name == 'pull_request' || github.event_name == 'push')

    steps:
      # ── Fast-pass when CodeQL was intentionally skipped ──────────────────
      # If no Python/JS files changed, codeql-analysis never ran.
      # We exit 0 here so the job completes as SUCCESS, satisfying the
      # branch protection required status check.
      - name: Auto-pass when CodeQL was skipped
        if: needs.detect-changes.outputs.run-codeql != 'true'
        run: |
          echo "⏭️ CodeQL was skipped (no Python/JS changes detected)."
          echo "✅ Security gate auto-passes — nothing to scan."
          exit 0

      # ── Fail-safe: block gate if analysis itself failed ──────────────────
      # If CodeQL was supposed to run but the analysis job failed (not skipped),
      # we should not silently pass — surface the problem.
      - name: Block if CodeQL analysis failed
        if: |
          needs.detect-changes.outputs.run-codeql == 'true' &&
          needs.codeql-analysis.result == 'failure'
        run: |
          echo "❌ CodeQL analysis job failed — security gate blocked."
          echo "Fix the CodeQL analysis errors before merging."
          exit 1

      # ── Main gate: fetch alerts and evaluate ─────────────────────────────
      - name: Check alerts & annotate PR
        if: |
          needs.detect-changes.outputs.run-codeql == 'true' &&
          needs.codeql-analysis.result == 'success'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // ── 1. Fetch open CodeQL alerts for this SHA ──────────────────
            const { data: alerts } = await github.rest.codeScanning.listAlertsForRepo({
              owner:     context.repo.owner,
              repo:      context.repo.repo,
              ref:       context.sha,
              state:     'open',
              tool_name: 'CodeQL',
              per_page:  100,
            });

            if (alerts.length === 0) {
              core.info('✅ No open CodeQL alerts for this commit.');
              return;
            }

            core.info(`Found ${alerts.length} open CodeQL alert(s).`);

            // ── 2. Bucket alerts by severity ──────────────────────────────
            const critical = [];
            const high     = [];
            const medium   = [];
            const low      = [];

            for (const alert of alerts) {
              const loc = alert.most_recent_instance?.location;
              const sev = alert.rule?.security_severity_level ?? 'unknown';
              const item = {
                sev,
                ruleId:   alert.rule?.id ?? 'unknown',
                desc:     alert.rule?.description ?? '',
                filePath: loc?.path ?? '',
                line:     loc?.start_line ?? '?',
                url:      alert.html_url ?? '',
              };
              if      (sev === 'critical') critical.push(item);
              else if (sev === 'high')     high.push(item);
              else if (sev === 'medium')   medium.push(item);
              else                         low.push(item);
            }

            // ── 3. Build comment body ─────────────────────────────────────
            function badge(sev) {
              if (sev === 'critical') return '🚨 **CRITICAL**';
              if (sev === 'high')     return '⚠️ **HIGH**';
              if (sev === 'medium')   return '🔵 **MEDIUM**';
              return '⚪ **LOW**';
            }

            const visibleFindings = [...critical, ...high, ...medium, ...low];
            const branch = context.ref?.replace('refs/heads/', '') ?? context.ref;

            let body = `## 🔐 CodeQL Security Findings\n\n`;
            if (critical.length) {
              body += `> ❌ **${critical.length} critical finding(s) — merge is BLOCKED.**\n\n`;
            } else {
              body += `> ✅ No critical security issues found! Merge is unblocked.\n\n`;
            }

            if (visibleFindings.length > 0) {
              body += `| Severity | Rule | File | Line | Description | Link |\n`;
              body += `|---|---|---|---|---|---|\n`;
              for (const f of visibleFindings) {
                body += `| ${badge(f.sev)} | \`${f.ruleId}\` | \`${f.filePath}\` | ${f.line} | ${f.desc} | [→](${f.url}) |\n`;
              }
            } else {
              body += `_No findings._\n`;
            }

            body += `\n---\n_CodeQL · \`${branch}\` · ${new Date().toUTCString()}_`;

            // ── 4a. Post / update PR comment ──────────────────────────────
            if (context.eventName === 'pull_request') {
              const prNumber = context.payload.pull_request.number;
              const marker   = '<!-- codeql-findings -->';
              const fullBody = `${marker}\n${body}`;

              const { data: comments } = await github.rest.issues.listComments({
                owner:        context.repo.owner,
                repo:         context.repo.repo,
                issue_number: prNumber,
                per_page:     100,
              });

              const existing = comments.find(c => c.body?.includes(marker));
              if (existing) {
                await github.rest.issues.updateComment({
                  owner:      context.repo.owner,
                  repo:       context.repo.repo,
                  comment_id: existing.id,
                  body:       fullBody,
                });
              } else {
                await github.rest.issues.createComment({
                  owner:        context.repo.owner,
                  repo:         context.repo.repo,
                  issue_number: prNumber,
                  body:         fullBody,
                });
              }
            }

            // ── 4b. Open tracking issue on direct push ────────────────────
            if (context.eventName === 'push' && visibleFindings.length > 0) {
              const issueMarker = `<!-- codeql-${context.sha} -->`;
              const issueTitle  = `🔐 CodeQL findings on \`${branch}\` — ${new Date().toLocaleDateString('en-CA')}`;
              const issueBody   = `${issueMarker}\n${body}`;

              const { data: existingIssues } = await github.rest.issues.listForRepo({
                owner:    context.repo.owner,
                repo:     context.repo.repo,
                state:    'open',
                labels:   'codeql',
                per_page: 50,
              });

              const existing = existingIssues.find(i => i.body?.includes(issueMarker));
              if (existing) {
                await github.rest.issues.update({
                  owner:        context.repo.owner,
                  repo:         context.repo.repo,
                  issue_number: existing.number,
                  body:         issueBody,
                });
                core.info(`Updated tracking issue #${existing.number}`);
              } else {
                for (const [name, color] of [['security','e11d48'],['codeql','f97316']]) {
                  try {
                    await github.rest.issues.createLabel({
                      owner: context.repo.owner,
                      repo:  context.repo.repo,
                      name,
                      color
                    });
                  } catch(_) {}
                }
                const { data: newIssue } = await github.rest.issues.create({
                  owner:  context.repo.owner,
                  repo:   context.repo.repo,
                  title:  issueTitle,
                  body:   issueBody,
                  labels: ['security', 'codeql'],
                });
                core.info(`Opened tracking issue #${newIssue.number}: ${newIssue.html_url}`);
              }
            }

            // ── 5. Workflow annotations ───────────────────────────────────
            for (const f of high) {
              core.warning(`[HIGH] ${f.ruleId}: ${f.desc} — ${f.filePath}:${f.line}`);
            }
            for (const f of critical) {
              core.error(`[CRITICAL] ${f.ruleId}: ${f.desc} — ${f.filePath}:${f.line}`);
            }

            // ── 6. Block merge on critical ────────────────────────────────
            if (critical.length > 0) {
              core.setFailed(
                context.eventName === 'pull_request'
                  ? `Merge blocked: ${critical.length} critical finding(s). See PR comment for details.`
                  : `⚠️ ${critical.length} critical finding(s) on ${branch}. Tracking issue opened.`
              );
            }
